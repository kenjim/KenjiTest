Global setup:

 Set up git
  git config --global user.name "Kenji Morishige"
  git config --global user.email kenji@kenjim.com
        

Next steps:

  mkdir KenjiTest
  cd KenjiTest
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:kenjim/KenjiTest.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:kenjim/KenjiTest.git
  git push -u origin master
      
